{
  "name": "alaarab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun run build && NODE_ENV=production bun server.ts",
    "build": "bun build ./index.html --outdir=dist --minify --sourcemap=linked --public-path=/ && bun build ./src/entry-server.tsx --target=bun --outdir=ssr-build --minify && bun scripts/prerender.ts",
    "prerender": "bun scripts/prerender.ts",
    "og": "bun scripts/generate-og.ts",
    "typecheck": "tsc --noEmit",
    "test": "playwright test"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router": "^7.9.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "^1.3.14",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^6.0.0"
  }
}
